# Increase network timeout to prevent installation cancellation on slow networks
network-timeout=60000

# Avoid strict peer-dependency errors during install
strict-peer-dependencies=false

# Auto-install missing peer dependencies
auto-install-peers=true
